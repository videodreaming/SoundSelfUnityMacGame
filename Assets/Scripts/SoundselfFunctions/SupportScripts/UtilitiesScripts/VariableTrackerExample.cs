using System;
using System.Collections.Generic;
using System.Reflection;
using UnityEngine;

/*
public class VariableTrackerExample : MonoBehaviour
{
    private int exampleInt = 0;

    // Update is called once per frame
    void Update()
    {
        exampleInt++;

        // Check if 'exampleInt' has changed since the last frame
        if (VariableChangeTracker.CheckVariableChange(nameof(exampleInt), exampleInt))
        {
            Debug.Log("exampleInt has changed!");
        }
    }
}
*/